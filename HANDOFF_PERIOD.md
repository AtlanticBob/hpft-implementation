# 交接：把控制周期降到 1ms 以下（2026-07-11）

这份文档是"降低控制周期"这条工作线的单一入口。目标是把方案 E 的控制
周期从 50ms 一路压低到 1ms 甚至更低，同时不让公平性、稳态精度、利用率
这些性能指标变差。整条环路是接收端每个周期测一次到达速率、算一次应得
速率、更新一次虚拟队列标记、把标记发回发送端，发送端跑一次响应律得到
新的速率、再把速率下发到执行器（RDMA 走 PCC，TCP 走 host EDT）。周期
越短，这一圈跑得越快，对政策变化和拥塞的反应就越及时。

先说清楚现在到哪一步了：**1ms 的控制环已经能跑（接收端实测 989 个
周期每秒），一个藏得很深的测量 bug 已经根治，但两条执行路径在 1ms 下
都还只能跑到 cap 的 66-84%，比 50ms 的 93% 差。所以"周期到 1ms"这一半
做到了，"性能不变差"这一半对两条路径都还没做到。** 实验室目前停在经过
充分验证的 50ms 配置上，1ms 的代码全部提交、可以用一个开关切回来继续
调。下面把已经查清楚的东西、还没解决的东西、以及接下来按组件优化的
路线，逐条讲清楚，让接手的人不用把我踩过的坑再踩一遍。

## 先读这些

1. 本文件。
2. `docs/latency_budget_1ms.md` —— 整条环路每一段延迟的实测数据。这是
   全部优化的依据：它告诉你哪一段慢、慢多少、能不能改。
3. `docs/control_period_1ms_status.md` —— 1ms 改造的详细现状，包括
   TCP 路径验证的结论和 V-scaling 的洞察。本文件是它的"向前看"版本。
4. `docs/rd_fairness_design_e.md` —— 方案 E 的设计正文（术语、机制、
   参数含义）。改控制律或标记前必须理解这里的语义。
5. 自动 memory `eurosys-rd-fairness-design`（每会话自动加载）里有这条
   工作线的浓缩进度。

## 已经查清楚的关键事实（不要再重新推导）

**延迟预算里哪些是硬地板，哪些能改。** PCC mailbox 的单次调用要阻塞
13.3ms，这是固件层的往返，公开 API 改不动，已经定为"合并式写"来绕开
（发送端把预算合并成大约每秒 75 次推给 mailbox，控制律仍然每个周期跑，
两者解耦）。representor 的 vport 字节计数器大约每 0.87ms 才更新一次，
这是物理下限——它决定了 1ms 量级可行、0.1ms 量级不可行。megaflow 的
硬件字节计数器有 1 秒的缓存，所以类构成比本来就只有 1 秒新鲜度，不需要
每个周期去拉。这些事实合起来的结论是：**测量面留在 DPU Arm 上、用
vport 计数器做快环，1ms 是能做到的合理终点；想再往下要把测量搬进
DPA，那是另一个数量级的工程。**

**一个藏得很深的测量 bug，已经根治。** 改造初期，稳定的 6G 流量被
接收端测成上下 ±60% 的抖动。查了很久才定位：慢环的 megaflow dump 如果
落在两次 vport 采样之间，就把"读到的字节数"和"记下的时间戳"解耦了——
某个周期把 dump 期间到达的流量也算进去了、时间却没算，于是速率虚高，
下个周期又虚低。修复是两条，都在 `rx_agent.py`：速率用固定样本数的
窗口而不是时间窗（时间窗对循环抖动不健壮，一个稍晚的周期会把唯一的
旧样本挤出窗口，导致除零和翻倍），以及把 vport 采样挪到每个循环的
最顶端、在任何慢工作之前，让采样节奏只由 sleep 调度决定。修完以后
50ms 下的表现回到和旧系统一致。这个 bug 是所有后续调参的前提，别把
它改回去。

**欠利用不是某一条执行路径专有的。** 我一开始怀疑是 PCC RP 的内环
耦合（RP 的设备码只在"喂进去的接收速率变化"时才做一次控制步，而 1ms
的测量平滑恰好去掉了 50ms 时 RP 依赖的抖动）。但把 TCP 路径也验证了
之后发现，TCP 在 1ms 完整环下同样只有 66-77%，和 RDMA 的 84% 是一个
病。TCP 走的是 host EDT、直接债务延迟，跟 RP 完全没关系。所以**病根
不在执行器，而在接收端标记的动力学在 1ms 下太敏感。** 这是重要的方向
性结论：别再往 RP 耦合上使劲，往标记和测量上使劲。

**V 的缩放本来就是错的，已经改对。** V 是"虚拟队列饱和触发满标记之前
容忍的超额积分量"，单位是比特。虚拟队列累积的是 ∫(r−e)dt，这个时间
积分在给定的墙钟窗口里跟 tick 周期无关，所以 V 必须是一个固定的比特
数，不能随周期缩放。我原来的公式让 V 正比于周期，1ms 的 V 就比 50ms
小了 50 倍，虚拟队列对测量尖峰敏感了 50 倍。修复是把 V 锚定到参考
周期（`rx_agent.py` 里已改，50ms 下数值不变所以对旧系统零影响）。改完
TCP 从 3.97G 升到 4.65G，是往正确方向走，但没走到头。

**还剩的核心矛盾：一套参数不能同时适配两条执行路径。** 把 V 调大以后
TCP 好了，但 RDMA 反而脱管跑到 18G——标记变迟钝了，R 一路涨到 tree
上限没人拦。原因是 RP（自带内环、会把突发平滑掉）和 EDT（直接按债务
延迟发包、突发结构完全不同）对同一个 V 和 β 的响应不一样。接收端测到
的 r，RDMA 侧和 TCP 侧的尖峰特征也不同。这是接下来要解决的主要问题。

## 接下来按组件优化的路线

整条环路可以拆成几个组件，每个都有独立的优化空间。建议按下面的顺序
做，因为前面的先解决能让后面的更好判断。

**组件一，测量（接收端 r 的平滑）。** 现在快环用大约 6ms 的固定样本窗
算速率，只够平滑 0.87ms 的计数器量化噪声，压不掉 TCP/EDT 那种几毫秒
到十几毫秒的突发结构——接收端因此偶尔测出 7G 的尖峰（真实 wire 只有
4.5G），这些尖峰触发虚假标记、把流钳在 cap 下方。第一件事就是把速率
平滑窗从 6ms 加到 20-30ms，用虚拟队列的积分能力换取更平滑的 r。虚拟
队列本来就是设计来吸收单周期噪声的，前提是 V 够大（V 已经改对了）。
要权衡的是 r 的响应延迟：窗口越大，r 反映速率变化越慢。20-30ms 对 1ms
的环来说仍然比 50ms 快得多，应该可以接受。这一步最可能一举压住尖峰，
建议从这里开始。改动点在 `rx_agent.py` 的 `HybridRates`，参数
`rate_window_s` 在 registry 的 e_params 里（现在 0.006）。

**组件二，标记灵敏度按执行路径分别标定。** 如果组件一压平了尖峰但两条
路径的最优 V/β 还是不同，就需要让接收端对 RDMA 类和 TCP 类用不同的 V
（甚至不同的 β）。这在设计上要小心：方案 E 的核心主张是"一条传输无关
的响应律"，per-class 的速率常数已经在 Q24 讨论过（允许，但记为张力）。
per-class 的 V 是标记侧的东西，性质类似，做之前要想清楚怎么在论文里
自圆其说，并且记录下来问用户。改动点在 `rx_agent.py` 的 `VQMarker`
和 `Scheduler`，参数结构要扩到 per-class。

**组件三，响应律的参数在 1ms 下重新整定。** 增量 A、乘性减系数 β、
app-limited 的窗口这些，现在是按周期机械缩放的（50ms 的值乘以周期比）。
机械缩放保持了墙钟行为，但 1ms 下的最优值不一定就是缩放出来的值——
比如 1ms 下标记响应快 50 倍，发送端可能不需要 MD 砍那么狠。需要在
组件一、二稳定之后，扫一遍 A/β 的参数空间，用 M1b/M2 那套场景做
"性能不劣化"验收（时间口径的收敛周期数、稳态均值、类间比例、利用率，
跟 50ms 基线对比）。改动点在 `tx_agent_e.py` 的参数加载段和 registry。

**组件四，TCP 的高重传要查清楚。** 1ms 下 TCP 的重传次数异常高（一次
20 秒的测试里超过 10 万次），这会直接压低 goodput，是 TCP 欠利用的
一部分原因，和标记动力学是两回事。怀疑是 host EDT 在 1ms 高频改 pace
的情况下丢包（旧的 opt3 EDT 是债务延迟、不丢包的，但重装后的行为要
核实）。先用 tcpdump 或 EDT 的计数器确认是不是 EDT 在丢，再决定怎么
修。执行器在 `tcp/bpf-opt3/`，host 侧的 pace 桥是
`tools/host/hpft_pace_shim.py`。

**组件五（可选，更激进），发送端合并写的节奏。** RDMA 预算现在按大约
13ms 合并推给 mailbox。如果组件一到三之后 RDMA 的均衡稳定了，可以看看
这个 13ms 的合并延迟对政策阶跃的响应有没有影响，值不值得进一步优化
（比如只在预算变化超过阈值时才推）。这是次要项，等主要问题解决再看。

## 怎么切到 1ms 去测、怎么恢复

registry 的 `e_params.period_ms` 是控制周期，现在是 50。切 1ms 就是
把它改成 1、重启两个 agent。相关的新参数：`ref_period_ms`（参数缩放的
基准，50，别动）、`slow_loop_ms`（慢环 dump 的节奏，200）、`log_every_ms`
（日志节流，20）、`rdma_push_ms`（RDMA 合并写节奏，13）、`rate_window_s`
（组件一要调的那个，0.006）。

测试前必读 `docs/ops_notes.md` 的实验卫生四条，尤其是：改政策先改 repo
的 registry 再下发（repo 是唯一真值），实验前把 soak 流量的 cron 注释
掉，实验批次之间重启 RP。测 RDMA 用 `~/hyperfront/perftest-26015/
ib_write_bw`（系统自带的会 core dump），测 TCP 用 iperf3；两端速率
自报之外，接收端 agent 的 jsonl 日志（`/tmp/hpft_rxagent_e.jsonl`）里
有每个周期的 r/e/s/vq，是诊断动力学的主要数据源。发送端的
`/tmp/hpft_txagent_e.jsonl` 有 R/pace/mode。RDMA 侧还可以用
`echo "0xdeb 0" > /tmp/rp_fifo` 内窥 RP 的 budget/level/measured-rate
（详见 ops_notes）。

TCP 路径在 DPU 重启后需要重装 host EDT（BPF pin 会丢），命令是
`sudo -n env PATH=/usr/sbin:$PATH /home/zhaoxiang/hyperfront/hpft-exp/
tcp_shaper/tools/tcp-shaper-apply --registry config/lab-tcp-registry.json
--egress-dev dpu1vf0 --qdisc-mode root-fq --bpf-object
tcp/bpf-opt3/hpft_tcp_edt_kern.o --apply`。

一轮调参结束、要把实验室交回稳定态时，把 period_ms 改回 50、
share_floor 关、vf0 的 MaxRate 恢复到 20G（售出额度），下发、重启
agent，用一条单流 RDMA 确认 18G@20G 正常，再把 soak 的 cron 恢复。

## 判断标准

这条线什么时候算成功：控制周期 ≤1ms 的前提下，M1b（单类 RDMA 6G cap）
和 M2（类间 1:1 与借用）两个场景的稳态均值、类间比例、利用率都不比
50ms 基线差（利用率允许在测量误差内，比例误差 ±10% 以内），收敛用
时间口径衡量、应该比 50ms 更快。达到这个就可以把 1ms 定为默认周期、
更新设计文档的参数章节、把这条线并回主线。
