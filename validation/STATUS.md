# Validation 状态（2026-09-08，令牌桶执行面，每场一遍）

本文件随每轮验证更新。场景与判据的定义与跑法在同目录 `README.md`，每场的报告在 `reports/<tag>.md`，图在 `fig/`。被测对象：接收端账本（`e_params`，`law=conf`）加发送端的令牌桶执行面（设计第 6 节），租户拥塞控制是执行面里的 DCQCN（缺省）与 Swift，TCP 是 Cubic（V6 换 BBR）。四台都在 HPFT 态、SR 重传、每 VF 50 G、交换机接收端口 `hpft_ecn`；这一轮 sn5600 处于单交换机形态（回环核心只在 V8 的两个臂里临时切上）。

## 总表

| 场景 | 贴合 | 账本 | 收敛 | 不塌 | 健康 | 执行面 | 一句话 |
|---|---|---|---|---|---|---|---|
| V1 稳态 incast，DCQCN | O | O | O | O | O | O | 24 个流集合 7.49–7.65 G（应得 7.67），合计 181 G，Jain 1.000 |
| V1 稳态 incast，Swift | O | O | O | O | O | X | 账本层与 DCQCN 相同；这一行是除法律时代的结果，令牌池上最差流集合贴合 0.949→0.974、判据 1 由 1/3 变 3/3（第三节） |
| V2 流集合加入/退出 | O | X | X | O | O | O | 五段稳态全对；RDMA 加入 1.55 s、退出 1.77 s，TCP 0.66/0.76 s；账本擦线 |
| V3 租户加入/退出 | O | X | X | O | O | X | 根层重划对；RDMA 加入 1.76 s、退出 1.05 s，TCP 0.17–0.86 s；账本与执行面擦线 |
| V4 需求变化与借用 | O | O | O | O | O | O | 出借 10.7 G、借入 36.8 G，两个沿 0.15–0.76 s |
| V5 单目的额度、零物理拥塞 | O | O | O | O | O | O | 六个流集合 8.16–8.29 G（应得 8.33），交换机 12 秒 26 个标记、零丢弃 |
| V6 换 CC（Swift + BBR） | X | X | X | O | O | X | 稳态低 3–7%，Swift 加入 3.9 s、BBR 退出 2.3 s |
| V7 与不受管流量共处 | O | O | X | O | O | O | 40 G 背景压端口时 24 个流集合各 5.95–6.07 G（按物理根算应得 6.13），两类都在容差内；根缩 1.27 s，回来 0.3 s |
| V8 账本管不到的核心瓶颈 | O | O | O | O | O | O | 核心 200 G 被 300 G 需求撞满时六个流集合各 29.9–30.3 G（应得 30.67），与拥塞控制单独运行的 30.0–30.5 G 相同，核心零丢弃 |

O 通过、X 不通过。**判断**：账本与执行面合起来的稳态承诺（贴合、不塌）在受管端口上的所有形态都成立，借用与收回也成立。不过的三样各有各的原因，都不改变份额：加入事件的 1.5–1.8 s 是账本还账（第三节），账本判据的 1.0–1.3 s 是同一件事的尾巴，判据 6 在 Swift 上的失败是桶模拟的毫秒粒度对逐往返变速的拥塞控制（第二节 V1）。

## 一、这一轮改了什么才跑出上表

**执行面的 QP 记录不再以硬件上下文为键。** DOCA PCC 交给算法的每流上下文不是一个 QP 一份：同一份上下文里会轮流出现两个 VF 的两个 QP 的事件，也会出现一个 VF 的 QP1 与它的数据 QP；上下文在 QP 销毁后不清零，下一场同号的新 QP 带着旧记录进来。以上下文为键时，一个流集合在册 5 个 QP、另一个只剩 2–3 个，串过去的按别人的 R 整形、没绑上的按未知额度跑，账本把受影响集合的 R 压到 1.9 G 去抵消。现在记录按 (vhca_id, QPN) 自建表，上下文只作缓存提示；每毫秒对照代理的映射表，流集合号变了就解绑重绑；沉默 2 s 解绑。`0xdf3` 回读给出上下文错配数、解绑数、绑定数（`docs/IMPLEMENTATION.md` 的 QP 归属一段）。

**没有注册表标签的流集合有了稳定的集合号。** sgpu03 的 vf4–vf7 在注册表里没有 flowtag，代理以前对这些流集合既不推预算也不推绑定，V3 的新租户整场按未知额度 4 G/QP 跑在 15.5 G；现在没有标签就用 (源, 目的) 字符串的 CRC32 作集合号，`distill.py` 用同一条规则反查。

**解析器只配对已连接的 QP。** `rdma res show qp` 会列出上一场残留的 ERR/RESET 态 QP，它们的号会被新 QP 复用，配出错的流集合；现在只取 RTS 与 RTR（perftest WRITE 的服务端 QP 停在 RTR）。

**runner 与判定器。** 执行面回读采样器 `tools/dpu/rp_sample.py` 逐槽等答复配对；runner 开跑前后清掉残留的 udp_blast（V7 的 40 G 背景曾因旧 sink 占着端口只发出 6.8 G）；判据 3 在 100 ms 均值上判；判据 6 只在全员取令牌的样本上判性质 1。

## 二、各场景

### V1 稳态 incast（DCQCN 与 Swift）

DCQCN：24 个流集合的归因速率 7.49–7.65 G（应得 7.67，偏差 −2.3% 到 −0.3%），RDMA 类 7.49–7.57、TCP 类 7.61–7.65；应用净荷 RDMA 7.00–7.04、TCP 7.36–7.44；接收端口 12 秒 4.46 亿帧里 ECN 标记 121 万（0.27%）、零丢弃。执行面自述：全员取令牌的样本里已整形之和/R 的均值 0.984–1.005、95 分位 ≤ 1.018；取令牌 QP 的拥塞控制速率之和是 R 的 62–85 倍——受管端口上 DCQCN 逐 QP 的速率停在线速，桶完全绑定，这正是设计 6.3 说的"R 以上归账本"。

Swift：账本层与 DCQCN 无差别（7.32–7.67，RDMA 类 7.32–7.48），标记只有 10.6 万（Swift 把端口队列压得比 DCQCN 低十倍）。判据 6 不过：已整形之和/R 的 5–95 分位是 0.77–1.43、中位 1.00。Swift 的 $c_i$ 是窗口除以往返时延，每个 RTT 变一次，而当时桶用除法模拟、分母每毫秒才重算，一个 QP 拿到的 $c_i R/S$ 里 $c_i$ 是此刻的、$S$ 是上一毫秒的，两者错开就让某一秒的快照偏离 R。**这个除法已经删掉，换成真正的令牌池**（第三节），这一行的数字是换律之前的。

### V2 流集合加入/退出

五段稳态全部在应得的 4% 以内：在位者 23 → 11.5 → 23 G，加入者 11.07–11.26 G，TCP 段同样。RDMA 加入 1.55 s、RDMA 退出 1.77 s 不过，TCP 加入 0.66 s、退出 0.76 s 过。退出沿上在位者从 11 爬回 23 G 用了 0.8 s，这是账本的上探速度；剩下的差在 RDMA 流集合本身的粗糙度：四个 QP 各自被硬件整形、桶每毫秒按取令牌的 QP 重分，100 ms 均值的标准差约 5%，±10% 的带子里 90% 的时间在带内这一条要多等半秒。账本判据两处 1.1–1.2 s 才清零、一段均值 1.33 ms，擦线。

### V3 租户加入/退出

根层重划正确：新租户出现时在位租户 23 → 11.5 G，新租户的 RDMA 独占 22.3–22.5 G（应得 23），TCP 加入后租户内两类各 11.2/11.5 G，TCP 退出 RDMA 回到 22.3，RDMA 退出在位租户回到 22.2–22.5。收敛：RDMA 加入 1.76 s、RDMA 退出 1.05 s 不过，新租户 TCP 加入 0.86 s、退出 0.17 s，租户内 RDMA 收回 0.75 s 过。账本两处恰好 1.0 s 才清零，执行面两个新租户集合的 95 分位 1.10，都是擦线。

### V4 需求变化与借用

RDMA 只要 10 G 时拿 10.68 G（应得 10.73，线上口径），TCP 借到 36.80 G（应得 37.66）；30 s RDMA 需求涨起来，RDMA 0.76 s 回到 24.1、TCP 0.56 s 退到 24.4（应得各 25）；60 s 回落，TCP 0.55 s 借回、RDMA 0.15 s 落定。账本全程清得掉，交换机零标记零丢弃。33.5 s 有一次 0.3 s 的抖动（RDMA 瞬间 12 G、账本 22 ms），在判据之内。

### V5 单目的额度、零物理拥塞

三个发送方各两类打同一个 50 G 的 VM：六个流集合 8.16–8.29 G（应得 8.33），账本清得掉，交换机 60 秒 3.15 亿帧里 26 个标记、零丢弃，发送端 CNP 增量为零。稀缺只来自政策额度，信号是接收端合成的，执行面自述已整形/R = 1.000。

### V6 换一种拥塞控制（Swift + BBR）

结构与 V2 相同，稳态每段都低 3–7%（RDMA 21.6–22.4 对 23，TCP BBR 21.8–22.4 对 23，加入段 10.7–11.2 对 11.5），比 DCQCN + Cubic 的 V2 低 2–4 个百分点；RDMA 加入 3.9 s、RDMA 退出 1.2 s、TCP 加入 0.67 s、TCP 退出 2.3 s。Swift 新来者从起步值爬到份额比 DCQCN 慢，BBR 在位者收回份额也比 Cubic 慢（BBR 探测带宽的周期是 8 个 RTT 一轮）；两者都是拥塞控制自己爬坡的速度，桶不替它们抬。差异超过了 README 写的 5% 门槛，说明"结果与 CC 无关"在稳态成立、在动态不成立。

### V7 与不受管流量共处（边界）

30–60 s 一股 HyperFront 不调度也不整形的 40 G UDP 压住接收端口（端口合计 185.7 G，其中背景 41.1 G）。账本把根缩到 146 G，24 个流集合各 5.95–6.07 G（应得 6.13）：RDMA 类 5.95–6.01（应得的 97%）、TCP 类 6.02–6.07（99%），都远在账本判出借方的 85% 容差之内，没有流集合低于 3 G；接收端口 90 秒 16.9 亿帧里 ECN 标记 546 万（0.32%）、零丢弃。根缩的那一下 RDMA 流集合 1.27 s 进带（账本 52 ms 的一次尖峰），背景停掉后 0.3 s 回到 V1 的份额。这一节的通过条件（不饿死、两类不低于 85%）满足；判据 3 只报数字。

### V8 账本管不到的核心瓶颈（边界，桶对拥塞控制单独两个臂）

这两个臂在切成双交换机的形态下跑（回环线两头切上、主机口分进 VLAN 101/102、核心出口整形 200 G），跑完切回单交换机（`run/campaign_20260908_b.sh`）。两个接收端各一个 VM 常驻（各 47.9 G，VM 上限），20–40 s 每侧再加两个 VM 的 24 QP RDMA，300 G 的需求撞 200 G 的核心。桶臂：六个流集合 29.88–30.26 G（应得 30.67），加入 0.87 s 进带、退出 0.16 s，账本全程为零（受管端口上没有拥塞），核心口 20 秒 9.08 亿帧里 ECN 标记 1.55 亿（17%）、零丢弃，DCQCN 在标记下把核心队列压在门限之下；六个流集合的应用净荷 28.0–28.3 G，常驻的两个 35.1/35.2 G。拥塞控制单独臂：29.97–30.51 G，加入 0.86 s、退出 0.17 s，标记 1.58 亿、零丢弃，净荷 28.0–28.5 与 35.0/35.1。两个臂的合计之差 0.3%，Jain 都是 1.000，没有流集合低于 3 G。执行面自述给出了设计 6.4 说的那幅图（`fig/V8_executor.png`）：20–40 s 取令牌 QP 的拥塞控制速率之和落到 R = 50 G 之下的 22–35 G，已整形之和跟着它走，桶不绑定；瓶颈撤掉后又回到 R 之上。设计对账本管不到的瓶颈的回答，即交给两边的拥塞控制在 R 以下处理，在这里成立，代价为零。这一节的通过条件（两臂合计之差 ≤ 5%、桶臂的 Jain 不低于拥塞控制单独臂、核心真的拥塞）满足。

## 三、加入为什么要 1.5 秒：账本给出保证的代价

V2 的加入沿上，在位者与新来者都在 0.4 s 内到达新份额 11.5 G，接着虚拟队列涨到上限、约 1.9 s 后才排空，这段还账把两边的速率压到 10.3–10.4 G，正好跌出 ±10% 带的下沿。判据量到的 1.5 s 就是这段还账。执行面与它无关：桶的五种记法（含已删除的除法与它的两个快照变体）全部落在 1.5–2.1 s。

**账本在加入的那一刻把额度发多了。** 把接收端每 10 ms 的原始记录逐拍重放，机制是这样：新流集合出现时它的 100 ms 平均到达量还在爬，出借判据（想要的比份额少就是出借方）分不清"我用不满我的份额"与"我还没起来"，于是把新来者判成出借方——出借方的 E 保持在自己的份额上，而它"让出来"的容量又通过水填分给了在位者，同一份容量被算了两次。

| 加入沿 | Σ E 峰值 ÷ C′ | 超发持续 | 到达量峰值 ÷ C′ | 虚拟队列峰值 | 还账期最低到达量 ÷ C′ |
|---|---|---|---|---|---|
| V2 t=9.8 s | 1.19 | 980 ms | 1.031 | 48 ms | 0.930 |
| V2 t=24.9 s | 1.24 | 130 ms | 1.031 | 58 ms | 0.915 |
| V3 t=34.5 s | 1.49 | 360 ms | 1.031 | 61 ms | 0.912 |
| V7 t=6.65 s | 1.42 | 380 ms | 1.035 | 89 ms | 0.921 |

**这 1.5 秒是闭环在做它该做的事，改不掉。** 把加入沿上账本积压最深的那个流集合逐拍看出来，机制是：份额被重算的那个流集合，在随后的几百毫秒里到达量是它新额度的 1.22 到 1.81 倍（V2 的两个沿 1.32、1.39；V3 三个沿 1.22、1.30、1.81；V7 1.45），账本照章记账，还账就是判据量到的那一段。份额刚变时没有任何流集合能已经落在新值上，所以这段超出是环路在走，不是租户在超用——但这两件事从到达量本身分不开，只能靠"离份额变动过去了多久"来分。

三条改法都试过，全部否掉：

| 改法 | 离线（重放录下的到达量） | lab |
|---|---|---|
| 从没到过份额的流集合不把份额借出去 | 超发精确消失（Σ E ÷ C′ 1.229 → 1.000），但账本积压从 30 涨到 85 ms | 没上 |
| 额度下降后冻结积分 | 关键的两个加入沿完全没动 | 没上 |
| 冻结到到达量够到新额度为止 | 毫无效果：流集合从第一拍起就在新额度之上，条件立刻解除 | 没上 |
| 份额一被重算就冻结积分 1 秒 | 积压峰值多数沿降到 1/2 到 1/6，十三个沿里六个的还账时长缩短 30% 到 75%，冻结占比只有 2.8% 到 9.5% | **明显更差**：四个事件全部劣于三遍基线（加入 2770 对 1257–1761 ms，TCP 加入 3770 对 658–1067 ms），判据 4 塌了——四个 RDMA 流集合从 32 s 起低于份额的一半，基线三遍都没出现过 |

最后一行是关键：**账本正是把端口压在容量之下的那个机制，蒙住它一秒，端口立刻被打过头，租户的拥塞控制接管，然后塌。** 离线重放能判断"账本的算术有没有按预期改变"，但判断不了闭环后果，因为到达量本身就是环路的输出——这一条要写在重放脚本的用法里。

所以第一条的结论是：**加入沿的 1.5 秒不是账本的缺陷，是账本给出保证的代价。** 唯一剩下的杠杆在发送端，让流集合落得离新份额更近、从而少一点可记的超出；这条 2026-08-30 试过一次（把新来者的起步值压低），因为另一个原因更差。要再碰它得当成一次设计改动来做，不是一次优化。

## 四、执行面的律：除法换成令牌池（2026-09-09 转正）

RDMA 硬件没有跨 QP 的共享桶，执行面自己维持一个。原来的做法是用除法模拟：$r_i = c_i R/\sum_j c_j$，分子在事件上读、分母每毫秒重算一次。对每个往返改一次速率的拥塞控制（Swift），两者错开，一个流集合的已整形之和绕 $R$ 摆 ±30%。

**现在是真正的令牌池，除法连同它的两个快照变体一起删掉了**（`0xcce <0|1|2> 22` 现在是 0 令牌池、1 等额封顶、2 等分；分母旋钮 `0xcce n 23` 随除法一并删除）：

$$r_i=\min\Big(c_i,\ \frac{R+\text{池存量折成的速率}}{N}\Big)$$

流集合有一个字节池按 $R$ 续满，每个 QP 在自己的事件上为刚过去的那一段按当时的限速给池付账；把成员加起来付的正是这个集合被允许发的量，所以池里存的是"$R$ 减去已批准量"的积分，由它导出的上限是一个不需要选增益的积分控制器。$N$ 是最近一毫秒发过包的 QP 数，每毫秒数一次。**没有任何一个 QP 读别的 QP 的状态**，每往返变一次的量不再进入任何需要跨流集合一致的算术——这正是分子分母对不上的根源。用不满自己那份的 QP 把字节留在池里，每个人的上限按份额抬高，重分配在同一个毫秒内完成。池的存量与欠账都封在四分之一个控制周期的预算上。

**转正的依据：三遍两臂共十八遍的 A/B（缩短后的场景）。** 判决只用互相印证的量具：接收端网卡的线上速率、账本归因、应用净荷；判据 6 只列不判（它的自述量不准，见第五节）。

| 场景 | 指标 | 除法，三遍 | 令牌池，三遍 | 分得开吗 |
|---|---|---|---|---|
| V1 Swift | 最差流集合贴合 | 0.954 / 0.949 / 0.949 | **0.974 / 0.974 / 0.974** | 令牌池三遍全高于除法三遍 |
| V1 Swift | 总利用率 | 98.0 / 97.8 / 97.7% | **98.8 / 98.7 / 98.6%** | 全分得开 |
| V1 Swift | 应用净荷 | 170.3 / 171.1 / 169.9 G | **172.6 / 171.8 / 172.2 G** | 全分得开 |
| V1 Swift | 接收网卡线上 | 22.24 / 22.20 / 22.14 G | **22.59 / 22.57 / 22.55 G** | 全分得开 |
| V1 DCQCN | 接收网卡线上 | 22.50 / 22.48 / 22.53 G | **22.67 / 22.69 / 22.67 G** | 全分得开 |
| V1 DCQCN | 贴合、利用率、净荷 | — | — | 重叠 |
| V2 | 四项 | — | — | 全部重叠 |

V1 Swift 的稳态贴合判据从 1/3 变成 3/3，其余判据两臂相同；V2 两臂一致（判据 2 与 3 两臂都是 0/3，是加入沿那 1.5 秒，与执行面无关）。十八遍里没有一项是令牌池明显更差的。数据在 `results/V{1,1swift,2}_{div,tok}_r{1,2,3}_20260909/`。

**四个实现上的坑**，都是量出来的：池存量要先给流集合再按在发的 QP 均分，不能每个 QP 各加一份（否则四个 QP 各拿满 $R$）；池深按一个毫秒算比流量能搬动的多三十倍，池永远满、上限永不生效；按上线字节收费在受管端口上没有反馈（线上恒等于 $R$），池停在起始值；欠账必须和存量一样封顶，否则一次超批准把池压到很负、整集合被钉在速率下限（V2 上 RDMA 掉到 1.6 G 对 23 G 的份额）。两种更严的记账（每毫秒巡查收费、巡查结清欠账）都是拿交付换自述，退回。

## 五、两条判据的量具

**判据 6 的自述量具已修好（2026-09-09）。** 原来的回读报的是"最后一次写下去的速率"，每秒采样一次——而这个量每几微秒就变一次，用一个瞬时样本估它的均值本来就估不准。现在每个 QP 在事件路径上累加"速率乘时间"，回读把这个计数交给主机、由主机除以它自己两次查询之间量到的间隔；邮箱处理里不读任何时钟（那里的时钟不是事件处理的时钟，拿它补最后一段会把答案打散到 0.91 至 1.58），也不按 QP 各报均值再求和（只出现十分之一时间的 QP 会把它那段的全速整个计进来）。

量具好不好，用"自述 ÷ 接收端归因"逐流集合看离散度（同一条律、只看四个 QP 都在册的流集合）：

| 场景 | 旧量具三遍的宽度 | 新量具的宽度 |
|---|---|---|
| V1 DCQCN | 0.120 / 0.600 / 0.440 | **0.042**（0.953 到 0.995） |
| V1 Swift | 0.266 / 0.559 / 0.099 | **0.037**（0.926 到 0.964） |

新量具窄一个数量级，而且从不报得比线上还高——自述是"编程的速率"，本来就应当略低于交付。中位数 0.93 到 0.97 说明线上比编程多三到七个百分点，这是编程速率与实际交付之间的真实差，现在稳定可测而不是被噪声淹没。

**它还暴露了一个旧噪声掩盖的真缺陷：绑定会漏 QP。** 一个流集合在册的 QP 数比同场其他流集合少，说明有 QP 从来没绑上；**没绑上的 QP 完全不受这个流集合的令牌池约束**，它按未知流额度跑，于是份额对它不成立，而这个流集合的自述也偏低（第一次看见时是归因的 0.481）。判据 6 现在会把它单独报出来，蒸馏对 20260909 的二十遍回算：

| | |
|---|---|
| 出现的运行 | 20 遍里 **16 遍** |
| 短的流集合 | 共 32 个（每遍 1 到 4 个，每场 12 个流集合） |

**这比我最初以为的"十二分之一"严重得多，是常态而不是偶发。** 两端网卡是干净的（本地四个 QP 全 RTS、对端全 RTR、号一一对应），执行面分配了记录却没绑（`g_q_bound` 比 `g_q_alloc` 少），所以丢在解析器到代理到映射表这一段。用 `quick.sh` 复现不出来（那里每个流集合是 5 到 6 个 QP、全部绑上，条件不同），所以改为在它真正发生的地方取证：采样器一旦看到某流集合的在册数低于同场众数，就把当时全部 QP 记录与绑定诊断写进 `results/<tag>/qpdump_<host>.txt`。

**判据 3 量到的一半是归因粒度。** RDMA 流集合的 100 ms 归因速率本身有 3.6% 到 5.5% 的标准差（TCP 是 0.8% 到 1.5%），稳态里就有 5% 到 10% 的格子落在 ±10% 带外。判据 3 要求窗内 90% 的格子在带内，对 RDMA 几乎没有余量：这一条量到的是接收端逐流集合归因的粒度，不是控制环。要让它只量控制环，得把带子放在实测均值周围而不是名义份额周围，或者把粒度放到 250 ms 以上。

## 六、下一步

1. **绑定会漏 QP**（第五节）：修好量具之后第一眼就看见了——一个流集合在册 3 个 QP 而不是 4 个，自述只有归因的 0.481。先查 `0xdf3` 的绑定诊断，再决定是解析器、代理还是执行面的记录表。
2. 加入沿的 1.5 秒已查清并结案（第三节）：它是账本给出保证的代价，不是缺陷，三条改法都试过并否掉。要再碰只能从发送端的落点入手，当设计改动做。
3. V6 的动态慢是拥塞控制自己的爬坡；如果论文要给"与 CC 无关"下结论，需要写清楚只在稳态成立。
4. V8 的通过条件里的拥塞证据是标记或丢包：DCQCN 在 ECN 下不让核心队列走到丢包门限，这一轮核心 17% 的帧被标记而零丢弃，README 已按此改。
