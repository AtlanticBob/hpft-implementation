# src_host src_vf dst_host dst_vf class count start_s end_s options   (times on the experiment clock, after the 5 s warm-up)
# V8 的核心链路版：两个接收端（sgpu02、sgpu04，都在 B 侧），发送端 sgpu01、sgpu03 都在 A 侧，
# 所有流都过 sn5600 内部那根 200G 的核心线（swp21↔swp25），它是两个接收端的账本都管不到的真瓶颈。
# 0–13 s 每个接收端只有一个 VM（核心上 100 G，不绑定）；13–27 s 每侧再加两个 VM 的 RDMA（需求 300 G 撞 200G 的核心）；27 s 退出。
# class=core 不是一条流：它告诉 distill.py 核心链路的容量（内层字节口径 184 G = 200 × 0.92），用来算应得值；runner 不动它。
# 每个 RDMA 流集合 24 个 QP（六个共 144 个）。RoCE 每个 QP 最多约 512 KB 未确认数据，所以核心口
# 队列的物理上限就是 QP 总数乘 512 KB，约 74 MB；核心口的出向门限是 35.2 MB（alpha_1 取 70.47 MB
# 池的一半），两倍余量，拥塞时必然丢包。QP 少于 70 个的话队列够不到门限，核心只标记 ECN 不丢包，
# 围栏拿不到任何丢包证据，置信度的授予就无从验证。
sgpu01 0 sgpu02 0 rdma 24 0 40 -
sgpu03 0 sgpu04 0 rdma 24 0 40 -
sgpu01 1 sgpu02 1 rdma 24 13 27 -
sgpu01 2 sgpu02 2 rdma 24 13 27 -
sgpu03 1 sgpu04 1 rdma 24 13 27 -
sgpu03 2 sgpu04 2 rdma 24 13 27 -
- - - - core 1 0 40 gbps=184
