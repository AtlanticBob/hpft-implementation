# M5c — 超售场景（售卖原则验证，design_e §3.6 v2.1 / Q23）

日期：2026-07-10。接收端 p1 降至 50G（C_root=48.5G），dst 四 VM 各配
20G 售出额度（Σ售出 80G > 容量，超售比 1.65）。验证 2026-07-10 定案的
云售卖原则：任何 VM 不超自身额度；超售竞争时按权重缩水；某 VM 低用量时
释放的量分给其他 VM 但各自仍被 20G 封顶。

| 阶段 | 实测 | 理论 |
|---|---|---|
| 4 VM 全速 | 11.94 / 11.93 / 11.93 / 11.97G（spread 0.3%，sum 47.8G） | 各 48.5/4 = 12.1G |
| vf0 自限 5G | vf0=5.29，其余 13.72 / 13.69 / 13.65G（全 <20G） | 其余各 (48.5−5.75)/3 ≈ 14.2G |

waterfill 的 per-VM cap = min(MaxRate, 需求) 天然实现该原则——无需任何
新机制。同一代码路径对称适用于发送端树（§3.6）。

注：perftest 老坑再证——`-D` 两端必须一致（vf0 分阶段跑需按阶段起匹配
的 server）。数据 `os_rx.jsonl`、`os_vf*.log`、时间戳 `os_*.txt`。
